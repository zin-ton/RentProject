package org.example;

public class Car extends Vehicle{
    public Car(String brand, String model, Integer year, Integer price, Boolean rented, Integer index) {
        super(brand, model, year, price, rented, index);
    }
}
